# 当前验证记录

- Windows离线pytest：290通过、2跳过（ROS消息序列化、未指定外部原始驱动），1条xacro弃用警告。另做干净归档测试，记录见交付说明。
- Python语法、XML模型、仿真/real控制边界与原任务回归：通过。
- 已使用工作区已有SciPy/xacro依赖修复离线测试环境；本机Anaconda基础环境仍不兼容，未修改全局安装。
- 新增真实后端测试覆盖：反馈时效、非零/半正定协方差、双指去零阈值、14个命令关节、闭合成功不等于持物、超时锁存、交接拒绝释放、禁止仿真服务依赖。
- HKV C++协议测试：编译并通过，覆盖开闭寄存器、目标力、错误从站拒绝、发送失败和断开不松爪。
- FR3反馈看门狗C++测试：编译并通过，覆盖重复帧超时、故障锁存、计数回绕与异常时钟；不等于ROS硬件插件编译通过。
- 离线基座/TCP拟合测试：通过，覆盖方向、退化数据、残差拒绝；不替代现场标定精度验证。
- 驱动在原prepare_driver输出上继续修订了伺服生命周期和HKV保持行为，旧dual_cell_adapter.diff仅记录原适配，不是本版完整差异。
- colcon、ROS graph、Gazebo、硬件：未执行，本机无可用 ROS 2/WSL。
- GitHub 发布：尚未完成。

Dockerfile/GitHub Actions为待运行的目标平台验证定义，不是已通过的构建结果。独立位姿来源须由现场真实相机提供，ArUco工具需要刚性带标记测试件和已验证误差。

vendor 为本地参考目录，不加入 Git 提交。独立构建所需的 HKV、Fairino 版本驱动及消息已放在 src。
